import 'package:flutter/material.dart';

class AppColors {
  // Background & Surface
  static const background = Color(0xFFF8F9FC);
  static const card = Color(0xFFFFFFFF);
  static const border = Color(0xFFEAECF0);

  // Text Colors
  static const textPrimary = Color(0xFF1D2939);
  static const textSecondary = Color(0xFF667085);
  static const textHint = Color(0xFF98A2B3);

  // Brand Gradients
  static const gradientStart = Color(0xFF245FF6);
  static const gradientEnd = Color(0xFF5EC4FA);

  // Status Colors
  static const success = Color(0xFF32D583);
  static const error = Color(0xFFF04438);
  static const warning = Color(0xFFFDB022);

  // Navigation
  static const selectedNav = Color(0xFF245FF6);
  static const unselectedNav = Color(0xFF98A2B3);
}